# Network metadata
id = "snapshot-example"
label = "Snapshot Example"

# Global properties (preset-level defaults)
[properties]
ambientTemperature = "20.0 C"

# Inheritance rules
[inheritance]
general = ["block", "branch", "group", "global"]

# Map property names to dimensions (for global properties not in schemas)
[dimensions]
ambientTemperature = "temperature"

# Unit preferences
[unitPreferences.Pipe]
length = "km"

[unitPreferences.Source]
pressure = "bar"

[unitPreferences.dimensions]
length = "m"
pressure = "Pa"
temperature = "°C"
